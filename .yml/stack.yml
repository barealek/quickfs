services:
  app:
    image: ghcr.io/barealek/quickfs:0.1.17
    command: "/app/api --static /app/dist"

  tunnel:
    image: cloudflare/cloudflared:latest
    command: tunnel run
    env_file:
      - prod.env
